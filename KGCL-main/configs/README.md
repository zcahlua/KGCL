# KGCL configuration

`default.yaml` mirrors the historical command-line defaults. Use `--config path/to/file.yaml` with `train.py`, `preprocess.py`, or `prepare_data.py`, then override individual values on the command line.
